# V7 固定主工作区与小版本开发约定

> 日期：2026-07-15
>
> Source commit：`a4b58d9`
>
> 当前分支：`dev/sage-v7`
>
> 固定工作目录：`/Users/zeromadlife/Desktop/tour-agent`

## 调整结论

Sage 后续不再要求用户为了联调在多个 feature 分支和 worktree 之间切换。仓库根目录已经从 `dev/sage-v6` 切换并固定为 `dev/sage-v7`，后续可直接在该目录启动前后端。

常规开发采用“小版本直提”方式：

1. 在 `dev/sage-v7` 实现一个可独立验证的小版本；
2. 执行受影响测试、生产构建和 `git diff --check`；
3. 给出“可提交 / 继续开发 / 需修复”结论；
4. 验证通过后直接提交到 `dev/sage-v7`；
5. 更新 Obsidian 复盘，并保持主工作区可以直接联调；
6. 阶段发布门禁完成后才把 V7 合入 `main`。

只有用户明确要求、出现并行开发冲突或高风险实验需要隔离时，才临时创建 `codex/*` 和 worktree。隔离工作由执行者自行合并、验证和清理，不要求用户切换。

## 本次迁移保留内容

- P2.2-A 持久批量知识任务提交 `f49c841`；
- 飞书 cc-connect 设计提交 `3ff96bf`；
- 飞书真实联调记录提交 `56e1892`；
- cc-connect 上下文与安全规则提交 `714ec35`；
- 小版本直提约定提交 `a4b58d9`；
- 主目录原有 `.cc-connect/`、`.codex/`、`CLAUDE.md`、部署脚本和临时文件均原地保留，没有被纳入本次提交或覆盖。

## Git 状态

- `dev/sage-v7` 已推送并与 `origin/dev/sage-v7` 对齐；
- 独立 V7 集成 worktree 已解除并清理；
- 根目录当前签出 `dev/sage-v7`；
- `dev/sage-v6` 分支保留为历史开发线，不再作为日常联调分支；
- `codex/fix-v6-9-timeline-contract` worktree 中仍有未提交测试修改，因此按保护原则保留，没有强制删除。

## 后续联调方式

在固定目录执行：

```bash
cd /Users/zeromadlife/Desktop/tour-agent
git branch --show-current
git status --short
```

分支应显示 `dev/sage-v7`。后续启动、测试、提交和服务端部署准备都以该目录为准。

## 下一阶段

下一小版本从 P2.2-B 开始，优先建立 Parser Registry 和统一文档解析结果契约，再分批接入 HTML/PDF、MinerU 与视觉模型。每个小版本保持可运行、可测试，不等待整个 P2.2-B 一次性完成后才允许联调。
